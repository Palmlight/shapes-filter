import React, { useContext } from "react";
import Layout from "../../components/Layout/Layout";
import { ShapesContext } from "../../context/ShapesContext";

const ShapesIndex = () => {
  //   const { state } = useContext(ShapesContext);

  return (
    <Layout>
      <div>orrkrkrkrk</div>
    </Layout>
  );
};

export default ShapesIndex;
